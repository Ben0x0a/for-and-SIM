#pragma once

// Single source of truth for tool identity, so every acquisition's manifest,
// meta file and HTML report record which build produced it.
namespace forandsim {

inline constexpr const char* kToolName = "For&SIM";
inline constexpr const char* kToolVersion = "0.2.3";
inline constexpr const char* kRepoWeb = "https://github.com/Ben0x0a/for-and-SIM";

} // namespace forandsim
